name: CHAT2 Smoke
on:
  push:
    branches:
      - chat2/atencao-fila-humana
    paths:
      - .github/workflows/chat2-smoke.yml
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo OK
